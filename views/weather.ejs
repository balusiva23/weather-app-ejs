<!DOCTYPE html>
<html>
<head>
  <title>Weather App</title>
  <!-- Include Bootstrap CSS link here -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>

  </style>
</head>
<body>
  <% if (error) { %>
    <p class="error-message"><%= error %></p>
  <% } else { %>
    <!-- <h1>Weather for <%= weatherData.city %></h1> -->
    <div class="card">
    <div class="weather-card">
        <h1>
          The Weather is Currently  <%= weatherData.weatherDescription %>
        </h1>
        <h6 class="fw-lighter">
            The temperature in <%= weatherData.city%> is <%=weatherData.temperature%>&deg;C
        </h6>
        <div class="weather-data">
          <p>Temperature: <%= weatherData.temperature %>°C</p>
          <p>Temp Min: <%= weatherData.tempMin %>°C</p>
          <p>Temp Max: <%= weatherData.tempMax %>°C</p>
          <p>Weather Description: <%= weatherData.weatherDescription %></p>
          <p>Wind Speed: <%= weatherData.windSpeed %> m/s</p>
          <p>Humidity: <%= weatherData.humidity %>%</p>

          <a href="/" class="btn btn-primary">Back to Home</a>
          <small class="text-muted" style="display: flex; justify-content: center;">Designed by Anton Francis
            Jeejo</small>
        </div>
      
    </div>
    </div>
   
  <% } %>
</body>
</html>
